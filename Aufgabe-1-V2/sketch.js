function setup() {
  createCanvas(windowWidth, windowHeight);

  background(235);

  stroke('magenta');
  strokeWeight(5);

line(0, 0, windowWidth, windowHeight);

  stroke('green');
  strokeWeight(15);

line(0, 0, windowWidth, windowHeight);
}